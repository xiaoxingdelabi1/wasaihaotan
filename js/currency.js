const Currency={UNITS:["wen","copper","silver","gold","cake"],UNIT_NAMES:["文","铜","银","金","饼"],RATIO:1e3,toWen(t){return Math.floor(t*Math.pow(this.RATIO,0))},toCopper(t){return Math.floor(t/this.RATIO)},toSilver(t){return Math.floor(t/Math.pow(this.RATIO,2))},toGold(t){return Math.floor(t/Math.pow(this.RATIO,3))},toCake(t){return Math.floor(t/Math.pow(this.RATIO,4))},getTotalWen(){let t=0;return t+=State.wen||0,t+=(State.copper||0)*this.RATIO,t+=(State.silver||0)*Math.pow(this.RATIO,2),t+=(State.gold||0)*Math.pow(this.RATIO,3),t+=(State.cake||0)*Math.pow(this.RATIO,4),t},addWen(t){let o=t;for(State.wen=(State.wen||0)+o;State.wen>=this.RATIO;)State.wen-=this.RATIO,State.copper=(State.copper||0)+1;for(;State.copper>=this.RATIO;)State.copper-=this.RATIO,State.silver=(State.silver||0)+1;for(;State.silver>=this.RATIO;)State.silver-=this.RATIO,State.gold=(State.gold||0)+1;for(;State.gold>=this.RATIO;)State.gold-=this.RATIO,State.cake=(State.cake||0)+1},subtractWen(t){let o=this.getTotalWen();if(o<t)return!1;let h=o-t;return State.cake=Math.floor(h/Math.pow(this.RATIO,4)),h%=Math.pow(this.RATIO,4),State.gold=Math.floor(h/Math.pow(this.RATIO,3)),h%=Math.pow(this.RATIO,3),State.silver=Math.floor(h/Math.pow(this.RATIO,2)),h%=Math.pow(this.RATIO,2),State.copper=Math.floor(h/this.RATIO),h%=this.RATIO,State.wen=h,!0},canAfford(t){return this.getTotalWen()>=t},format(t,o=!1){const h=Math.floor(t/Math.pow(this.RATIO,4));t%=Math.pow(this.RATIO,4);const e=Math.floor(t/Math.pow(this.RATIO,3));t%=Math.pow(this.RATIO,3);const a=Math.floor(t/Math.pow(this.RATIO,2));t%=Math.pow(this.RATIO,2);const r=Math.floor(t/this.RATIO);t%=this.RATIO;const s=[];if(h>0&&s.push(`${h}饼`),e>0&&s.push(`${e}金`),a>0&&s.push(`${a}银`),r>0&&s.push(`${r}铜`),(t>0||0===s.length)&&s.push(`${t}文`),o)return s.join(" ");const i=s.filter(t=>parseInt(t)>0);return i.length>0?i.join(" "):"0文"},formatState(){return this.format(this.getTotalWen())},formatShort(t){return t>=Math.pow(this.RATIO,4)?`${Math.floor(t/Math.pow(this.RATIO,4))}饼`:t>=Math.pow(this.RATIO,3)?`${Math.floor(t/Math.pow(this.RATIO,3))}金`:t>=Math.pow(this.RATIO,2)?`${Math.floor(t/Math.pow(this.RATIO,2))}银`:t>=this.RATIO?`${Math.floor(t/this.RATIO)}铜`:`${t}文`}};